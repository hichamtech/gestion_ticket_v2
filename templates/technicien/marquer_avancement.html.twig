{% extends 'technicien.html.twig' %}

{% block body %}
{{ form_start(form) }}
<div class="jumbotron">
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Marquer') }}</button>
</div>
{{ form_end(form) }}


{% endblock %}